<header class="app-header">
    <a href="{{ path('app_home') }}" class="app-logo">
        <img src="{{ asset('img/logo-3il.png') }}" alt="Logo 3iL"/>
    </a>

    {% if 'app_home' != app.request.get('_route') and 'app_legal_' not in app.request.get('_route') %}
        {% include '_layout/default/_navigation.html.twig' %}
    {% endif %}

    <div class="app-sitename">
        <span>Programmes Experts</span>
        <small>Référentiels RNCP</small>
    </div>
</header>
