{% set flashes = app.flashes %}

{% if flashes %}
    {% for type, messages in flashes %}
        {% for message in messages %}
            <div class="app-alert {{ type }}" data-controller="alert">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
{% endif %}
